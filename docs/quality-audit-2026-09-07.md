# 代码质量审计与夜间批次清单（2026-09-07 凌晨）

> 目标：高内聚低耦合 + 大文件拆分 + 逻辑重构 + 单测补齐。
> 与 docs/quality-plan-2026-09.md（P0~P7）衔接；本文件是执行层工作清单，每批完成后更新状态。

## 覆盖率基线（scripts/coverage_test_runner.sh，2026-09-07 实测）

- 全库 TOTAL：行覆盖 10.6%（6422 函数中 5741 未覆盖）、区域 10.85%。
- **2026-09-07 早晨复测：行覆盖 17.14%、区域 16.49%**（重构会话 Batch 11~17
  真身直测收口 + 夜间 B 系列后；剩余未覆盖为编排胶水/GCD/NSView 层，按三通道
  模型归真机 E2E 验收域）。
- **口径说明（重要）**：Runner 单测（无环境变量默认模式）只能覆盖纯函数/可注入逻辑；
  编排类文件（AX/yabai/AppleScript 编排）天然 0%，由真机 E2E（VIBEFOCUS_*_E2E）验收。
  三条测试通道：Runner 直测（真实实现单测）/ Standalone 镜像（自包含）/ 真机 E2E（行为）。
- 高覆盖样例：ToggleEngine+Restore 94.8%（分支 100%）、TerminalGridPlanner 88.9%（分支 100%）、
  TerminalAutoRestorePlan 92.9%（分支 100%）——「纯函数内核分支 100% + 编排 E2E」模式成立。

## 文件规模 Top（>300 行）

| 文件 | 行数 | 状态/计划 |
|---|---|---|
| TerminalGrid/TerminalGridController.swift | 1055 | **B1 已拆**：→ 6 文件（主编排 266 + SpaceDelivery 168 + Restore 268 + Capture 127 + TargetResolve 136 + Automation 121），提取纯函数 tty 解析/捕获排序/恢复帧规划（去重 restore 与 autoRestore 重复块）+ 12 条 Runner 断言 |
| Window/WindowManager+MoveWindow.swift | 554 | 重构会话域（Batch 6/7 MoveToMainPipeline 已推进），避让，观察其后续 |
| Settings/SettingsView+TerminalGridSection.swift | 536 | B3 候选：UI 文件，拆 Tab/子视图 + 表单校验逻辑提纯 |
| App/SoundManager.swift | 515 | B2 候选：音效域（有 ProjectSoundResolverTests 基础），拆播放引擎/音效选择/工程解析 |
| Toggle/ToggleEngine+Restore.swift | 444 | 覆盖已 94.8%，P1 已收敛，暂不动 |
| Space/SpaceController+Recovery.swift | 402 | B4 候选：SA 状态机域，改前先补镜像测试 |
| Hook/ClaudeHookModels.swift | 381 | 已有 HookModelsFullTests，查缺口再定 |
| App/AppDelegate+MenuAndInstance.swift | 362 | B5 候选：菜单/单实例小而独立 |
| Settings/DesignSystem.swift | 351 | 色板 token 文件，内聚良好，不拆 |
| App/VoiceAnnouncementManager.swift + RestoreOutcome | 317+28 | 覆盖 59-75%，B6 候选：补缺口测试 |

## 纪律

- 每批次：独立 worktree → 拆分/重构 → 提纯逻辑补穷尽单测 → 三门禁（构建零警告 +
  Runner 全绿 + run_all_tests）→ 合并推送 → 本清单更新。
- 并行避让：重构会话域 = Window 编排（FrameWriteExecutor/FloatSettle/MoveToMainPipeline/
  阶段状态机）；本清单不动这些文件，B 前先 fetch 查 origin/main 近期提交。
- E2E 同机互斥锁（P5）在岗；动窗口行为的批次加跑对应 E2E。

## 冲刺收官（2026-09-07 08:00）

- **覆盖率**：全库行覆盖 10.60% → **15.84%**（+49% 相对提升）；函数覆盖 6433 中新增 1000+ 可测函数入覆盖。
  B13~B18 六轮镜像转直测后，WindowMove+Decision 80.9%、ClaudeHookModels 80.8%（区域）、
  TerminalContext+Helpers 75%——提取单元函数级≈100%，编排路径由真机 E2E 家族验收。
- **Runner 直测断言**：278 → **867/867 全绿**（+589 条，全部真实实现直测、零镜像漂移；含 Batch 20 LayoutFrameCalculator 14 断言 + Batch 27 鉴权 13 断言 bcf1c4c + B28 Overlay 偏好层/Space 解码 17 断言 90d5b13 + B29 yabai 环境探针/绑定决策 18 断言 df2ac5a + B30 解码去重助手/语音模式 5 断言 010b1d3 + B31 Doctor 取证纯逻辑 10 断言 e485523 + B33 路径注入/路由提纯 9 断言 79028dc + B34 SettingsUI 提纯前置 13 断言 29b5b63 + B35 section 拆分/请求契约提纯 12 断言 7cef427 + B36 提示音门控/项目音效解析 12 断言 bda04ec + B37 能力自检/屏幕映射/网格库 22 断言 d44376c + B38 共存探测/热键/队列策略 19 断言 b54d1da + B39 切换反馈/标题脚本 12 断言 f4a5d32 + B40 错误文案/状态枚举收尾 5 断言 069762e + B41 提权模板/Doctor 端到端 7 断言 3233816 + B42 偏好契约回环 5 断言 6abfaea + B43 坐标纯函数 10 断言 ce0d3d1 + B45 热键校验提纯 4 断言 16ebc1a + B46 音效解析计划 6 断言 7f3e94a + B47 Doctor 报告全段落 6 断言 17c4a80）。
- **结构**：1055 行巨石 → 六模块；编排页/提示音段拆分；恢复帧规划去重；模型与编排分层。
- **真 bug**：volume 必填解码静默重置用户偏好（单测先行实锤修复）。
- **死测试清理**：FocusStepsCalculationTests（镜像函数已从源码删除）。

## 批次台账

| 批次 | 内容 | 状态 |
|---|---|---|
| B1 | TerminalGridController 1055→6 文件 + 纯函数提取（parseWindowTTYMap/sortedByReadingOrder/restoreTargetFrames 去重）+ 12 断言 | ✅ 2026-09-07 |
| B2 | SoundManager 模型块拆出 SoundPreferencesModels（515→393）+ **实锤并修复 volume 必填解码潜伏 bug**（旧 JSON 静默重置用户偏好）+ 真实实现兼容解码 15 断言 | ✅ 2026-09-07 |
| B3 | SettingsView+TerminalGridSection 拆四文件（536→137/214/87/99）+ summaryText/selectionDetailText/steppedGap 提纯 + 13 断言 | ✅ 2026-09-07 |
| B4 | SA 恢复状态机真实实现测试补齐（recoveryVerdict 6 + autoRecoveryAllowed 冷静期矩阵 6 + saProbeVerdict 4 = 16 断言，Runner 直测优于镜像）| ✅ 2026-09-07 |
| B5 | VoiceAnnouncement 模板插值/队列策略转 Runner 直测（消镜像漂移，8 断言）；AppDelegate+MenuAndInstance 审计结论=纯菜单粘合无需拆分 | ✅ 2026-09-07 |
| B6 | ClaudeHookModels 数据契约真实实现 23 断言（payload 双事件键别名/session 别名+trim+空拒绝/嵌套 ctx snake_case；TerminalContext 五因子绑定判据+isRemote；Response snake_case 编码）| ✅ 2026-09-07 |
| B7 | Hook 窗移决策树 Runner 直测 20 断言（守护顺序逐条+边界：超龄阈值/pidMatches nil 容错；httpResponse 映射表 8+1）——路由唯一事实源消镜像漂移 | ✅ 2026-09-07 |
| B8 | Space 投递七分支决策表 Runner 直测 9 断言（含 nil 查询失败容错两分支）——+SpaceDelivery 文件从 0% 抬升 | ✅ 2026-09-07 |
| B9 | SettingsView+SoundSection 318→3 文件（85/119/110）+ SoundSectionText/规则兜底音效提纯 6 断言 | ✅ 2026-09-07 |
| B10 | SessionWindowRegistry 查找级联隔离库直测 6 断言（直命中/pid 有效者优先/别名通道/DB 兜底层实证/markCompleted 联动）——新 env 门控 VIBEFOCUS_REGISTRY_E2E=1（须配隔离 DB）| ✅ 2026-09-07 |
| B11 | 终端上下文匹配族（tty 归一/命令-标题匹配/ps basename/iTerm UUID/注入防御 allowlist）+ Claude 窗口定位两级策略，Runner 直测 18 断言 | ✅ 2026-09-07 |
| B12 | GridTargetCode.parse 全形态/非法输入 + TerminalSelectionResolver.resolve（手动优先/auto 频次/空回落）Runner 直测 9 断言 | ✅ 2026-09-07 |
| B13 | float 脱管决策（含惰性不触查询不变量）/refocus 候选选择/outcomeLabel 四分支/retryable/源屏预切三态，Runner 直测 18 断言 | ✅ 2026-09-07 |
| B14 | restore 结局→播报计划总映射 Runner 直测 10 断言（spaceExact 三态/文案 nil 语义/成败音效通道）| ✅ 2026-09-07 |
| B15 | Quartz/Cocoa y 互转 + MoveCooldownRegistry 冷却纯决策/剩余秒数取整 Runner 直测 7 断言（无记录→0 语义对齐实现）| ✅ 2026-09-07 |
| B16 | walkToTerminalPID 谓词注入行走直测 9 断言（起始即终端/上溯/深度上限/ppid≤1 断链/自环/深度防御 + 注册表静态集合）| ✅ 2026-09-07 |
| B16.5 | soundType 兜底接线修正——规则未选音效时 Picker/试听共用 effectiveSoundType 单一事实源 | ✅ 2026-09-07 |
| B17 | Hook 脚本生成器不变量直测 9 断言（hooks JSON 合法性+恒注册事件/远程安装脚本 host 插值与 machine_label 归一/helper 端口与上下文采集）| ✅ 2026-09-07 |
| B18 | YabaiErrorClassifier 直测 8 断言（六类别/大小写不敏感/多类命中优先级）| ✅ 2026-09-07 |
| B19 | 镜像存活审计：70 个 Standalone 逐符号核对，**删除 1 个死镜像**（FocusStepsCalculationTests 镜像的 calculateFocusSteps 已从源码删除，测试在测自己的副本）；其余存活 | ✅ 2026-09-07 |
| B20 | SettingsUI.swift（244 行 34 个 @State 状态枢纽）审计结论=拆分属高风险 SwiftUI 状态重构且无单测面，缓办并记录；AppDelegate+MenuAndInstance=纯菜单粘合无需拆分 | 审计完成 2026-09-07 |
| B21 | SettingsView+ClaudeHookSection 334→265+89（Codex 区段独立）+ CodexInstallPresentation 展示映射提纯 5 断言 | ✅ 2026-09-07 |
| B22 | LAN 远程绑定持久化 Runner 直测（env 门控）3 断言：set nil 丢弃/active 过滤/独立 defaults 域；旧字典迁移路径留镜像覆盖（同进程 UserDefaults 缓存语义不适合进程内断言）| ✅ 2026-09-07 |
| B23 | TerminalUsageTable 直测 7 断言（record 幂等累加/lastAt 单调/minCount 过滤/14 天半衰排序/编解码回环与坏数据回退）| ✅ 2026-09-07 |
| B24 | 注册表状态操作隔离库直测 8 断言（markCompleted 置位+清别名/reactivate/touch 推进/空白消息拒覆盖/remap 迁移/clearAllBindings 清空）| ✅ 2026-09-07 |
| B25 | shellQuoted 单引号转义直测 3 断言（惯用法四字符序列/原样包裹/空串）；cellCommand 分支已由并行会话直测覆盖，本批去重不重复添加 | ✅ 2026-09-07 |
| B26 | IPS 崩溃报告解析直测 5 断言（首行头丢弃/多行 JSON 保留/非法与空输入回退）| ✅ 2026-09-07 |
| 热修复 | B26 合并（68cb44b）冲突解决误删 IPS do 块闭合 `}`——main 编译断裂（`private` 非 local 报错），阻塞全部并行会话；814f63a 补括号 + B24 var→let 警告修复，合并并行 Batch 20 后三门禁全绿（构建零警告 + Runner 623/623 + run_all_tests 75/75）推送 78d22b2。**教训入册：合并冲突删标记行后必须跑构建门禁再推送** | ✅ 2026-09-07 |
| B28 | 缺口审计（全 Sources 类型 vs 测试符号对照）后 Overlay 偏好层 + Space 解码漂移直测 17 断言：legacy 迁移补默认/字段保真/非法回退、CodableColor 键集锁定+回环保真、IndexPosition 6 形态映射互异、YabaiSpaceInfo is-visible Bool/Int 四态、YabaiDisplayInfo 解析（90d5b13）| ✅ 2026-09-07 |
| B29 | yabai 环境探针全注入直测（三层编排 L1/L2/版本 trim、parseSpaces 宽松语义、locateBinary 候选序）+ YabaiEnvironmentProfile 派生语义穷尽（spaceMoveTrusted 保守策略=v7 float 事故判据）+ decideSessionBindingStep 转直测消镜像，18 断言（df2ac5a）| ✅ 2026-09-07 |
| B30 | ScreenIndexPreferences.load 四源回退链：CF/UserDefaults 两源逐字重复的 decode→enforce→legacy→save 块收敛为 decodeWithLegacyFallback 单一助手（行为逐项对齐，savesLegacyUpgrade 参数供测试避副作用）+ VoiceAnnouncementMode 映射锁定，5 断言（010b1d3）| ✅ 2026-09-07 |
| B31 | Doctor 取证纯逻辑镜像转直测 10 断言：parseJournalLine 三事件行+容错（install 无 pid 占位/未知 kind/junk→nil）、accessibilityFlips 翻转捕获（nil 轴跳过不阻断）、unmatchedLaunches 配对抵消+at 排序（外部击杀实证）、runtimeAXFlipLine 版面守卫（e485523）| ✅ 2026-09-07 |
| B31 后覆盖率复测 | 行覆盖 12.88%（27267 行基数；B19 基线 15.84% 口径相同但分母已扩张——并行功能开发新增大量编排/SwiftUI 代码 + env 门控注册表测试不计入普通运行）。结构指标持续向好：**18 个文件行覆盖 100%**（LayoutFrameCalculator/ToggleFocusBranching/ToggleTriggerGate/ConditionPolling/FloatSettle/YabaiErrorClassifier/OverlayRefreshPolicy/Decision 三兄弟等纯内核族全部满格），函数级满格面更广 | 复测于 2026-09-07 |
| B32 | 依赖注入改造拔除编排层零覆盖根因：SessionWindowRegistry `private init` → `init(store:)`（三文件 12 处 WindowStateStore.shared 硬引用改走注入属性，直构临时 SQLite 即整链直测，无需 env 门控）；CodexHookPreferences 路径/脚本参数化 + install 清理合并段提纯为 mergedHooks 纯函数；直测 21 断言（isInstalled 四态/清理精准/合并保真+开关裁剪；init 剪枝/bind 拒绝/状态操作落库经真临时库），Runner 697→718（ff73ad9）| ✅ 2026-09-07 |
| B33 | Claude 偏好路径注入（claudeSettingsPath/Dir home 注入、isHookInstalled(at:)、uninstall 全参数化+removesHelpers 测试开关）+ SessionStart 前置分流提纯 decideSessionStartRoute 纯函数（handler 双通道收敛单主体）+ 直测 9 断言（安装→卸载回环经临时 settings）；ClaudeHookServer 审计结论=剩余 0% 属 GCDWebServer 第三方编排、DI 价值低缓办（79028dc）| ✅ 2026-09-07 |
| B34 | SettingsUI 拆分前置扫清：34 @State 的决策逻辑提纯为可测纯类型——提示音规则四操作从 SoundManager 单例下沉 SoundPreferences 纯结构（mutating，越界静默语义保留）、节流/免打扰表单钳制同步下沉、端口表单内联三元提纯 ClaudeHookPreferences.clampedUserPort（0=恢复默认）；SoundManager 五操作改委托行为不变；直测 13 断言。**缓办项解除：SettingsView 现可按 section 低风险拆分，状态决策均有纯类型单测兜底**（29b5b63）| ✅ 2026-09-07 |
| B35 | SettingsView section 拆分落地：混杂双域的 SettingsView+Helpers（261 行）拆为 Installations + HookTest；SettingsUI+Helpers（179 行）拆出 SessionLists（余纯展示助手）；HookTest 提纯 buildHookRequest 请求契约与 hookResponseVerdict 四态裁决（nonisolated 纯函数，测试免网络）；直测 12 断言。**B20 缓办项就此闭环：Settings 模块无 300 行以上视图文件，全部按域内聚**（7cef427）| ✅ 2026-09-07 |
| B36 | 全新缺口扫描（符号对照升级：区分零覆盖与仅镜像）后提示音双决策转直测：SoundPlayGate.decide 免打扰四语义（跨午夜/起闭右开/同日窗/无效配置）+ 硬静音优先级 + 节流取整边界（UTC 固定时区注入）、ProjectSoundResolver.resolvedType 首命中/非法跳过/路径归一匹配/全局回落，12 断言（bda04ec）| ✅ 2026-09-08 |
| B37 | 镜像/零覆盖三件套转直测：BuildCapabilities 二进制安全搜索契约（needle 三段命中/summary 登记序/missing 缺失清单——部署互踩 drift 自检）、ScreenLayoutMapper 纯函数全语义（y 翻转/scale 双向约束/胶囊带几何/网格预览等分）、TerminalGridStore store 注入直测（增改删/latest）+ TerminalGridPreferences 钳制回环（显式 0 间距持久=旧 bug 回归锁、标准域先存后还原），22 断言（d44376c）| ✅ 2026-09-08 |
| B38 | 仅镜像/零覆盖收尾批：WindowLayoutManagerProbe.evaluate 双通道探测（name 兜底/运行即安装/conflictSummary 拼接）、HotKeyConfiguration 显示串与冲突表互异 + 默认 ⌃Q 不撞表回归锁、TerminalSelectionResolver.supportLevel 三级别映射、VoiceAnnouncementQueuePolicy.appendedQueue FIFO/满丢最旧/容量防御/值语义、SpacePreferences 默认值与回环，19 断言（b54d1da）| ✅ 2026-09-08 |
| B39 | 并行会话新增镜像转直测：GridSpaceSwitchFeedback.message 三态如实反馈（空工作区失败说明原因不许静默——用户报告回归锁）、TitleEditor 脚本决策层增量加严（转义/定向寻址双哨兵/front 回退/verdict 大小写敏感/诊断 target_gone），12 断言（f4a5d32）| ✅ 2026-09-08 |
| B40 | 双口径扫描收尾：VoiceAnnouncementError 四类 LLM 错误文案互异+状态码插值、SpaceAvailability/LogLevel rawValue 契约，5 断言；审计注记=ToggleRecordStore 为协议抽象（DI 本体）属扫描假阳性，其余零覆盖均为 SwiftUI/App 生命周期/shell 编排豁免项——**可单测面清账完成**（069762e）| ✅ 2026-09-08 |
| B41 | 编排内嵌模板提纯：executeWithAdminPrivileges 的 AppleScript 模板构造（转义防注入+提权包装）提取为 makeAdminShellScript 纯函数（照 TitleEditor 模式，决策与执行分离）+ Doctor.report DoctorPaths 全注入端到端直测（临时 exits.jsonl → 生命周期/最近死亡/无配对现形三段），7 断言（3233816）| ✅ 2026-09-08 |
| B42 | 仅镜像清单最后三类型直测：ClaudeHookEventType 四事件 PascalCase + WindowMoveReason 三原因 snake_case（线上 JSON 契约）、VoiceAnnouncementPreferences 默认实例 Codable 回环、TitleEditorPreferences 双开关默认开+显式关闭；教训入册=Optional 链比较中 `.none` 解析为 Optional.none 而非枚举同名 case，断言须显式限定枚举类型，5 断言（6abfaea）| ✅ 2026-09-08 |
| B43 | CoordinateKit 纯函数补齐直测（原仅 E2E 门控消费、普通运行零覆盖）：originDrift/sizeDrift 曼哈顿漂移和（2.16a 第十二刀唯一公式）、isSizeConverged/isFrameConverged 漂移和判据回归锁（逐轴合计超调不许收敛——apply/PostMove 自我打架史）、clampFrame 尺寸收窄+位置夹回、isOnMainScreen 中心点归属，10 断言（ce0d3d1）| ✅ 2026-09-08 |
| B44 | 大文件按域拆分：ClaudeHookModels.swift（381 行混装窗口模型与 Hook 数据契约两域）拆为 HookWindowModels.swift（窗口身份/状态/ToggleRecord，192 行）+ 瘦身后 ClaudeHookModels.swift（EventType/TerminalContext/Payload/Response，195 行）；纯文件搬移零行为变更，两域类型均有 Runner 直测兜底（28ee38f）| ✅ 2026-09-08 |
| B45 | HotKeyManager.validate 提纯为 nonisolated static validationError(for:)（修饰键要求 + knownConflicts 查表唯一事实源），两处调用点静态化行为不变；Runner 内同语义镜像 hotKeyPassesSystemConflicts 删除、校验路径直测真身（无修饰键拒绝/冲突携带原因/默认 ⌃Q 合法/shiftKey 不算修饰），4 断言（16ebc1a）| ✅ 2026-09-08 |
| B46 | resolveSound 解析映射提纯为 SoundManager.soundResolutionPlan 纯函数（none/系统命名音/Bundle 资源名/自定义文件四通道 + 缺失降级 Hero + 显式路径优先级），resolveSound 只剩按计划执行；直测 6 断言免 IO 直锁映射（7f3e94a）| ✅ 2026-09-08 |
| B47 | Doctor.report 全段落端到端补齐（B41 三段之外的六段）：辅助功能授权时间线（当前状态+翻转计数）、疑似外部击杀计数、致命信号现场（存在 size/缺失不存在）、.ips 报告列表、keepalive 尾部回显、构建能力标记段恒在场——DoctorPaths 注入 + 临时夹具零真身 IO，6 断言（17c4a80）| ✅ 2026-09-08 |
| B48 | 双份判据漂移修复：CodexHookInstaller.cleanVibeFocusHooks 内联仅按 command 匹配（url 形态旧条目漏删），统一改调 HookSettingsComposition.stripVibeFocusEntries 唯一判据（url 精确 OR command 含脚本路径），targetURL 传递链补齐 clean/mergedHooks/uninstall 三签名；行为变化=Codex 安装/卸载现会清 url 精确匹配旧 HTTP 条目（与 Claude 侧对齐），断言更新 6 处（c0b4b6e）| ✅ 2026-09-08 |
| B49 | CoordinateKit 按纯净度拆分（330→171+175）：纯坐标数学（标识符/y 换算/clampFrame/漂移判据族，仅 CoreGraphics+Foundation、零 AppKit 触碰）与 NSScreen 查询半区（主屏帧/索引互转/NSApp 枚举）分文件——落实原文件「纯坐标数学」设计声明；纯文件搬移零行为变更，B43/B15 直测兜底（01cdb99）| ✅ 2026-09-08 |
| B50 | keepalive 崩溃裁决判据根治（第三次实证误报后）：2026-09-08 02:40 安装重启（SIGTERM）期间并行派生进程 touch 共享 fatal 文件（mtime 变、size 0→0）再次误判 60s 冷却——(mtime,size) 任一变化判据收敛为「仅 size 差分」（真崩溃必为 CrashSignalHandler O_APPEND 追加、size 必增；mtime 只随行取证不裁决；absent≡0 归档语义；入册误报史 2026-07-12 ×3 + 09-06 + 09-08 全部 size 不变）；生成器函数化 write_wrapper/write_plist + BASH_SOURCE 守卫（可 source 模板库，source 不触碰 launchd）+ 四个运行时注入缝（VIBEFOCUS_KEEPALIVE_OPEN_BIN/_COOLDOWN/_FATAL_LOG/_KLOG，缺省=生产行为不变）；KeepaliveWrapperDecisionTests 以假 open 二进制驱动生成物 7 场景 28 断言（生成器契约/mtime 触碰不误判/真崩溃 respawn+冷却重拉收摊/陈旧复活不误判/缺失→建空文件不误判/无变化基线/缺失→追加判崩溃）+ 真实决策日志隔离性总断言 | ✅ 2026-09-08 |
| 备注 | Tests/XCTest/ 套件在 CLT 环境从未可执行（playbook 2.10），属死重——**2026-09-08 已向用户征询裁决并附事实**（72 文件/10306 行；Package.swift:65 以专用 target 引用但本机不可执行；所锁逻辑已全部被 Runner 814 条真身直测覆盖且更新），用户未即时答复，默认维持保留现状；任何时刻用户回复「删」即 fork worktree 清理 + 三门禁推送。git 历史可找回，未来换完整 Xcode 亦可从历史复活 | 征询于 2026-09-08 |
| 覆盖率复测（B39 后） | 行覆盖 **16.93%**（27371 行基数，函数 23.62%）；较 B31 后复测 12.88% **+4.05pp**——分母微增下覆盖行增长超 1100 行，B32~B39 注入化/直测化与并行会话直测共同贡献；18 文件 100% 满格继续有效 | 复测于 2026-09-08 |
| 覆盖率复测（B50 后） | 行覆盖 **18.09%**（27469 行基数，函数 27.10%、区域 24.88%），较 B39 后 **+1.16pp**——B41~B50 直测 + 并行会话 minimap 几何匹配/标题脚本工作共同贡献。热点结构复核（≥80 cov-lines 且 <40% 清单）：头部几乎全为 SwiftUI 视图豁免类（OverlaySection/ClaudeHookSection/PermissionsSection/LANSettingsView 等 Settings/App 视图文件群）；非豁免头部均有入册结论——SpaceController+Recovery 7.54%（fork/admin 编排本体，纯决策 B4 已直测）、ClaudeHookServer 6.46%（GCDWebServer 第三方编排 B33 缓办）、SoundManager 8.97%（拆分否决+决策 B2/B34/B36/B46 已提纯）、CrashSignalHandler 0%（async-signal 上下文本体不可测）、WindowManager+MoveWindow/TerminalGridController+Restore 0~4%（AX/yabai IO 编排，纯内核已满格）。**与 B40 清账结论一致：无可单测面新增漏洞；后续增量继续走「决策提纯→Runner 直测」模式（随功能演进），文件级行覆盖数字不再作为批次目标** | 复测于 2026-09-08 |
| B52 | SA 恢复按域拆分 + admin 模板双份判据收敛：SpaceController+Recovery.swift（406 行，最后一批混装四域的大文件）拆为 +Recovery.swift（216，手动入口/探针/静默直载/提权编排）+ +SARecoveryState.swift（113，结局分类+退避策略纯函数+持久化+用户可见状态收敛）+ +SARecoveryAdmin.swift（104，AppleScript 模板+前台/后台提权执行）；纯文件搬移零行为变更，纯决策 B4/B41 直测兜底；**判据收敛**=scheduleBackgroundAdminRecovery 内联重写的转义+包装模板（与前台 makeAdminShellScript 构成双份判据，B48 同类漂移风险）改调唯一事实源；可见性代价=loadRecoveryState/recordRecoveryState/scheduleBackgroundAdminRecovery 三个方法 private→internal（跨文件调用所需，与 SoundManager 否决案的「存储属性放宽」不同类，代价可接受） | ✅ 2026-09-08 |
| 大文件终审（300+ 行全部入册，「拆大文件」目标收官） | B52 后 Sources/ 共 11 个 300+ 行文件逐一终审：SoundManager 416（拆分否决：private 存储属性放宽代价>收益；决策已全提纯直测 B2/B34/B36/B46）、ToggleEngine+Restore 395（已拆 +Restore+Decision 测试锁，本壳=依赖全注入编排，Runner 穷尽）、AppDelegate+MenuAndInstance 362（B20=纯菜单粘合）、DesignSystem 351（UI token 单一事实源=用户认知锚点，不拆）、WindowStateStore+Database 326（单一持久化域，B32 临时库直测）、Doctor 317（六段单域，DoctorPaths 注入+端到端 B41/B47）、VoiceAnnouncementManager 317（编排壳，模板/队列/文案已提纯 B5/B38/B42）、HotKeyManager 316（Carbon 编排，机制族已分文件+validate 提纯 B45）、AppDelegate 313（生命周期粘合豁免）、CrashSignalHandler 310（async-signal 安全上下文，拆分风险>收益）、WindowManager+MoveWindow 306（move 编排骨架，纯内核族满格直测）——**全部给出维持/已拆结论，无一混装多域；后续仅随功能演进按需新增内聚文件** | 终审于 2026-09-08 |
| B53 | hook 热路径 fatalError 退让（P1 红线：破坏性兜底清查续轮）：代码异味扫描（try!/as!/fatalError 全 Sources 清零核查）实锤 HookEventHandler+SessionStart 两处「决策表契约」fatalError——决策表演化引入跨通道 case 时一次 hook 请求即击穿整个菜单栏应用；改为诚实 500 上报（新纯函数 decisionContractViolationResponse：code=decision_contract_violation/通道名进文案/sessionID 回显）+ 错误日志 + registry 事件描述，应用继续活着；Runner 直测 +2 断言锁契约（870→872）；init(coder:) 惯用法豁免；扫描结论=try! 与 as! 在 Sources 中本就为零 | ✅ 2026-09-08 |
| B54 | install.sh 重启竞态加固（2026-09-08 02:40 安装实证 -609：pkill 异步 + 立即 open，旧进程死透前 LaunchServices 拒绝，新包当场没被拉起）：照 B50 模式函数化——wait_for_process_exit 轮询原语（kill -0、0.2s 间隔、超时不杀进程只上报）+ restart_app（pgrep 取 pid → pkill → 等待全退（上限 5s，超时打 WARN 继续，keepalive 兜底）→ open）+ BASH_SOURCE 守卫（可 source 模板库，source 零副作用）；InstallRestartHardeningTests 生成物级行为测试 9 断言（source 守卫隔离/已死 pid 立即通过/窗口内退出受控耗时/超时路径返回 1 且耗时符合轮询粒度/多 pid 混合）；另做主线程阻塞模式全扫（main.sync/DispatchSemaphore/waitUntilExit/Thread.sleep）——全部有界或有文档（ShellRunner 2s 超时、settle 族可注入 sleep 已直测、AppDelegate 启动路径 ≤1s 带升级终止），无新雷 | ✅ 2026-09-08 |
| B55 | bundle id 单一事实源收口（判据漂移最高危级：身份漂移）：全仓审计发现 **5 个脚本各自内联写 CFBundleIdentifier，其中 3 个写退役 id com.vibefocus.app**（install.sh/dev-build.sh/package_release.sh），而装机现状与 run.sh/build-release.sh = canonical com.openai.vibe-focus——退役 id 正是 docs 记载「误启旧副本回到无修复行为」陷阱的制造机。收口动作：①三脚本 plist 对齐 canonical；②install.sh 降级为 run.sh 纯委派器（消除双套构建/装包/签名实现；其缺失 NSAppleEventsUsageDescription/哈希跳过/部署锁/审计/全新 inode 的风险随之清零）；③run.sh 版本读取路径修复（曾指 Sources/AppVersion.swift 不存在 → VERSION 恒 0.0.0）+ B54 等待原语迁入（重启的生产事实源）+ 可 source 守卫；④uninstall.sh 补 canonical id 清理（TCC 行/defaults/Preferences/Caches 四处，退役 id 残留清理保留）；⑤Sources 三处 fallback 字面量对齐（通知名 com.vibefocus.app.open-settings 与 keepalive label 历史契约保留不动）；BundleIdentityContractTests 17 断言锁死契约（退役 plist 值零存在/四写入方 canonical/install 纯委派/版本路径/Sources fallback/通知与 label 不误伤/uninstall 双 id）。Runner 872→888（并行会话三屏回归 +16 断言并入） | ✅ 2026-09-08 |
| B56 | Tests/Runner/main.swift 按域拆分（6337 行/全仓最大文件/并行冲突震中——B26 热修复与多起双插同锚点冲突的现场）：结构审计=5 个假依赖类 + 单一巨型 `runAllTests()`（内含 harness 局部声明 passed/failed/check/window）+ E2E 锁尾区。变换：harness 提升为 `@MainActor final class RunnerHarness`（main.swift 保留假依赖类+尾区，6337→280 行），134 个测试块按域切成 **14 个 `extension RunnerHarness` 域文件**（RestoreOrchestration 977/PureSweepB 852/PureSweepA 767/ConvergencePipeline 631/RegistryStore 483/其余 9 文件），do-block 逐字搬移零内容变更、调用顺序不变；**等价验证=拆分前后 Runner 输出逐行 diff 一致（896 checks 同序同结果）**；构建期补切分器漏带的 `@testable import VibeFocusKit`。收益=并行会话新增测试落到对应域文件，main.swift 冲突震中退役；后续新增域测试直接新建域文件或入对应 extension | ✅ 2026-09-08 |
| B57 | 新约定固化进 AGENTS.md（多会话环境的护栏沉淀）：①单一事实源契约——bundle id canonical/com.openai.vibe-focus、退役 id 不得进新 bundle、install.sh 纯委派、装包重启只改 run.sh；②Runner 测试布局——main.swift 只放 harness/假依赖/E2E 锁，新增测试入对应域 extension 或新建域文件并在 runAllTests 登记；③脚本行为测试模式——可 source 函数库 + 注入缝 + 生成物行为测试（B50/B54/B55 模式命名范例）；Tests/e2e/README.md 补 B56 后 E2E 代码位置指引。纯文档批次，代码零变更 | ✅ 2026-09-08 |
| B58 | bundle id 字面量提纯为常量（B55 收官补刀）：Sources 三处 `?? "com.openai.vibe-focus"` fallback 字面量仍是三个可各自漂移的副本，收敛为 `AppIdentity.bundleID` 单一常量（Sources/App/AppIdentity.swift，注释引用脚本侧契约）+ 三调用点替换；Runner 直测 +2 断言（常量=canonical/非退役 id，896→898）；BundleIdentityContractTests T5 扩为 4 断言（字面量副本归零/常量引用 ≥3/常量定义在位且值 canonical/退役 fallback 零存在）；ShellRunner vs YabaiClient.execute 双实现甄别结论=语义已对齐且各有 P-INST 取证标记，属有意识分层不合并 | ✅ 2026-09-08 |
| B59 | 计划文档与门禁约定一致性收口：quality-plan P6「后续步骤 2」windowless 评估补记结论 ✅不建（评估早已出、步骤列表仍挂开放态）；P7 残留的第二行「Batch 3/4 立项/并行推进中」并入收官行（FrameWriteExecutor/阶段状态机已落地，见 Batch 6/8）；AGENTS.md 门禁第 3 条升格——Runner 从「涉及 restore 链路才跑」改为无条件必跑（B56 起为主断言基线 896+ checks 秒级），消除「改到 Runner 覆盖面但只跑 Standalone」的漏网窗口 | ✅ 2026-09-08 |
| B60 | Sources 死代码全量清查（启发式：函数名全仓出现次数 ≤ 定义次数）：8 候选甄别——6 个豁免（makeNSView/updateNSView=NSViewRepresentable 见证、windowDidBecomeKey/ResignKey/WillClose=ObjC selector、makeBody=ButtonStyle 见证，均为运行时回调不可静态见调用），1 个 makeBody 同前；实锤删除 **TerminalAutomationScript.isSupported(appBundleID:)**——dda1edb 初始大提交诞生即零调用，且与 TerminalRegistry（声明在案的终端单一事实来源，9 终端超集）语义分叉（硬编码 2 终端）；git 历史可找回。**附带发现（留给域所有者，本批不动）**：Terminal/iTerm2 id 内联判断散落 ≥5 处（Automation/Restore×2/Registry/isSupported），自动化域仅认 2 终端 vs Registry 9 终端属真实语义差异，收口需域决策；该域为并行会话 Batch 41 活跃区，按「在途不碰」纪律移交 | ✅ 2026-09-08 |
| B61 | 漂移镜像实锤转直测：镜像路径新鲜度扫描（Standalone 头部 `Mirrors:` 路径 vs 磁盘）实锤 **SoundAndTitleTests 双料漂移**——①头部路径 Sources/Window/TitleEditorPreferences.swift 已搬至 Sources/TitleEditor/；②镜像的 3 字段 SoundPreferences 是 B9 加节流/免打扰字段前的旧快照、镜像的 Codable 版 TitleEditorPreferences 在 Sources 里根本不存在（真身是 UserDefaults 门面）——镜像在测不存在的形状。处置：删除漂移镜像；真实类型未覆盖的自身契约（CompletionSoundType 7 cases/rawValue/displayName 全表/isBuiltin 分区/Codable 回环/非法值拒绝）转 RunnerSoundVoiceHookTests 直测 +6 断言（898→904）；SoundPreferences 解码韧性 B2 已直测、TitleEditorPreferences 行为 B42 已直测，不重复。另：Sources TODO/FIXME/HACK 清零确认 | ✅ 2026-09-08 |
| B62 | playbook 文档双源消除：现行运维约定（测试布局/门禁/bundle id 契约/脚本行为测试模式）以 AGENTS.md 为唯一事实源，playbook 头部加交叉引用注记、历史叙事按日期保留原貌不随现状改写（4e2b3bf）；main 集成态复核=4cba70f 三门禁已在本批前验证全绿 | ✅ 2026-09-08 |
| B63 | 生产代码卫生四维合扫（收官轮）：①`print(` 全 Sources 仅 `--diagnose` CLI 输出与生成 hook 脚本模板内（正当用途，零散落）；②隐式解包可选（IUO）零；③空 catch 块零；④Tests/Standalone 最大文件 531 行（GridTargetLogicTests），无巨文件。**审计矩阵就此全覆盖闭环**：异味/阻塞/死代码/身份判据/大文件（源码+测试两侧）/镜像新鲜度/TODO/覆盖率/计划文档/AGENTS 约定/playbook 单源/生产 print/IUO/空 catch——全部有结论、有测试或移交记录 | ✅ 2026-09-08 |
| B64 | B60 移交项落地——网格自动化终端身份单一事实源（并行清扫线 Batch 42 收官后域空闲，解锁）：`TerminalAutomationScript` 新增 `automationBundleIDs`（恰为 iTerm2 + Apple Terminal）/`usesITermDialect`/`isAutomationSupported` 三事实，网格域三处内联 id 判断（Automation 建窗 / Restore 观测 / Restore injectScript switch）全部收敛；行为逐分支等价（unsupported → nil 不变）；语义边界用不变量锁定——**自动化支持集 ⊆ TerminalRegistry 终端识别集**（Registry=「是不是终端」9 终端超集 vs 本集合=「有没有为它写方言」，刻意不同）。TitleEditor/WindowManager 侧的 id 判断属各自特性支持矩阵，刻意不动（全域统一=错误耦合）；Runner 直测 +5 断言（904→909） | ✅ 2026-09-08 |
| B65 | 零覆盖静态符号扫描（Sources 全部 static func × Runner/Standalone 命中对照）44 候选甄别，补测三件：①`SpaceController.resolveVisibleSpaceIndex`——源码注释自称 "extracted for testability" 却从未有测试消费（displayIndex/spaces nil 与空、目标屏可见命中、不可见/他屏/display 缺失/is-visible 缺失过滤、多可见取首命中、首命中 index 缺失不回退，新域文件 RunnerSpaceIndexTests 10 断言）；②`TerminalAutomationScript.cocoaBoundsTuple`——Quartz→Cocoa 四元组的机器无关不变量（四整数格式/x 就近取整/两侧独立取整容差 ≤1/高度保持容差 ≤1/翻转方向=整数平移恰 −100/零尺寸退化，6 断言）；③`ClaudeSessionLocator.latestSessionID` 加 `home:` 注入缝（默认 NSHomeDirectory 行为不变、唯一调用点走默认）+ 临时目录直测（30 天窗外滤除/恰在界上含端点入选/非 jsonl 即使最新也滤/目录缺失降级 nil/点分 stem 原样保留，6 断言）；其余 41 候选甄别为豁免——ps/lsof/AppleScript/SQLite/文件 IO 包装（findVia*/getProcessComm/recordExit 族/install* 族/loadFromSQLite/queryJSON* 等）归真机 E2E 或注入式编排、runPerspectiveGuard=日志+计时粘合（内层 refocusPerspective 已穷尽直测）、Doctor fileState/formatDate=报告 IO 粘合；已知可测余量留档：shellPID（runner 可注入的 ps 解析判定）可供下批 | ✅ 2026-09-08 |
| B66 | B65 留档余量落地：`ClaudeSessionLocator.shellPID`（tty 登录 shell 选取，runner 可注入的 ps 解析判定）零覆盖转直测——runner 注入假 ps 输出，8 断言锁定：调用契约（/bin/ps -t + /dev/ 前缀剥离/无前缀透传）、混合行全滤（login 包装/claude CLI/垃圾行/裸 pid）、`-zsh` 剥前导 `-`、路径形 basename、pwsh 属 shell 集、最小 pid 胜出、空输出/非零退出码/ps 缺失三路降级 nil；零新增生产代码（判定本体早已是注入式纯解析） | ✅ 2026-09-08 |
| B67 | HotKey 域漂移镜像又一条转直测（B61 同法）：Tests/Standalone/HotKeyDisplayTests.swift 整套复制生产形状（struct + 56 个 kVK 常量 + displayKey/modifierDisplay），测的是副本而非真身——真身改动不会红。处置：①`displayKey` 62 行 switch 重构为 `displayLabels` 数据表（58 键，展示契约以数据形式存在，表外键码回落 "?"，from(event:) 语义等价）；②展示契约迁 RunnerHotKeyDisplayTests 直测——displayLabels 全表字典逐键比对（多一条/少一条/改任何文案都红）+ 修饰键固定序 ⌃⌥⇧⌘ 经 displayString 复合通道锁定 + Codable 回环/JSON 字段/Equatable+Hashable + 默认值 ⌃Q/⌃⌥⌘M 与 knownConflicts 9 条契约；③Hook UPS 响应映射字段级收口 +3 断言（六决策 ok/200/sessionID 透传、常量分支文案逐字、结果响应文案透传）；④镜像文件删除（Standalone 82→81），XCTest 变体按既定决策留沉默路径。Runner +15 断言，与 B66 合并后 939→954 全绿。盘点注记：Standalone 75 文件带 `Mirrors:` 头属 script 模式文档惯例、非全为漂移，后续按 B61/本批法逐域甄别。插曲：refactor/quality-b65 worktree 被并行会话侵入写入 space-index WIP，按「在途不碰」整体撤离，本批改走唯一命名 worktree 完成 | ✅ 2026-09-08 |
| B67 | （并行会话 hotkey 线完成，本行由 B68 会话补记保持台账连续）HotKey 展示契约转直测——漂移镜像退役 + displayKey 数据表化，见 7edd8e2 | ✅ 2026-09-08 |
| B68 | 镜像新鲜度复扫（B61 后再轮）实锤 **2 死镜像 + 3 漂移镜像**，全部退役并转真身直测：①`TerminalAppRegistryTests`——镜像的 `Sources/Hook/TerminalAppRegistry.swift` 已死于 ccc11a7 终端列表统一进 TerminalRegistry，镜像在 re-declare 自己的副本自测（纯死镜像）；②`ShutdownSnapshotTests`——`Sources/Toggle/ShutdownSnapshot.swift` 已死于 3566882 restore 重写，同上（现机制 ToggleRecord/TerminalGridSnapshot 均有 Runner 直测）；③`ToggleSaveValidationTests`——三镜像中 `frameWithinTolerance`（位置≤tol 且尺寸≤2tol）**锁的是已废弃判据**（现行=CoordinateKit 漂移和 isSizeConverged/isFrameConverged，B43 已直测），shouldRejectOrigFrame/RestoreDecision 副本转真身；④`ToggleDecisionRoutingTests`——decideRestore 决策树副本+mirrorRoute/mirrorIsOnMainScreen 副本（ToggleRoute 已有 Runner 直测）；⑤`ToggleRecordTests`——isValid 副本且头部路径已漂移（仍指 ClaudeHookModels.swift:147，B44 已搬 HookWindowModels.swift:186，头部自供「源码若变须手改」即漂移陷阱自白）。新建 Runner 域文件 `RunnerToggleDecisionTests` 直测真身 22 断言：shouldRejectSave 六边界（中心在主屏拒/上方负 y/右/下/CGRect max 缘排除/nil 主屏）、decideRestore 六 case+守护顺序短路锁（moveToMain 先于 record 查询）、isValid 七断言（Quartz→Cocoa 换算双包含+target 中心恰落 max 缘边界）、isOnMainScreen 注入式重载三态。run_all_tests 82→76 文件、Runner 954→976 | ✅ 2026-09-08 |
| B69 | Hook 模型/脚本域漂移甄别处置（B61/B67 家法；机扫 81 文件 54 引用产 4 嫌疑全落 Hook 域）：①HookEventDecisionTests 三形状全死——decideBinding/BindingDecision ⊃ Batch 19 decideSessionBind（Runner 已直测）、isInCooldown 旧参标签 ⊃ MoveCooldownRegistry.isInCooldown（Runner 已直测）、logLevelForDuration ⊃ logOperationDuration 内联（IO 日志粘合豁免）→ 整删；②ClaudeHookPayloadTests + HookModelsFullTests——**镜像把 WindowMoveReason 锁 2 cases 而真身 Batch 14 已加 user_prompt_submit**（穷举断言对真身失效实锤）、payload 解码段测本地夹具而非 ClaudeHookPayload.init(from:) → 整删，真身契约迁新域 RunnerHookModelsTests 直测 22 断言（eventType 穷举/回环/非法拒绝、payload 双 event 键/sessionId 别名/trim/三重拒绝/terminal_ctx 嵌套/扩展字段、response 线格式 session_id 键名+nil 键省略、WindowIdentity/TerminalContext Codable 回环）；③HookScriptContentTests——generateHelperScriptContent(lanMode:) 签名漂移（真身改读 LANHookPreferences）+ generateMachineLabel/generateHookConfigJSON 真身已内联 → **生产行为等价提纯**：lanMode 参数测试缝（默认入口读偏好不变，P-INST-144 直测免 UserDefaults）、machineLabel(forHost:)/hookConfigJSON(host:port:token:machineLabel:) 提为命名事实源（generateRemoteInstallScript 消费同源），镜像删。叠加并行线退役后 Standalone 76→72；Runner 与并行 B68 合并后 976→998 | ✅ 2026-09-08 |
| B70 | 实例方法零命中扫描（231 个零命中 func，B65 静态符号扫描的补维度）甄别出完整零覆盖纯判定族——**DisplayWorkArea 学习型显示器保留区**（Sources/TerminalGrid/DisplayWorkArea.swift，P40UG 副屏 25px 隐形菜单栏保留区自愈的唯一事实源、重排/恢复共用）：（原编号 B69 与并行 Hook 域线撞号让先合者改 B70）新建 Runner 域文件 `RunnerDisplayWorkAreaTests` 直测 22 断言——merged 逐边 max（学习只更保守）/plannedFrame 扣减与面积扣穿回落（含恰好扣 0 的 guard >0 边界）/inferInsets 七断言（贴规划缘 0.5px 判定、顶行同向推离、任一格可达即无钳制、噪声阈 1.5、maxInset 200 异常防御、nil 读回跳过、最小推离+取整）/probedInsets 只缩不涨与可达归零自愈（单探测窗同覆盖顶左两缘）/learnedInsets+store 临时 suite 注入回环（多屏表隔离、归零摘键、坏数据 zero 兜底）。其余 229 个零命中实例方法初判为 AX/yabai/NSView/语音 IO 编排（归 E2E 域），全部留档名单 | ✅ 2026-09-08 |
| B71 | 零直测欠账清偿收官（B41/B42 双尾巴）：Runner 覆盖对照扫出残留零直测符号——①B42 清扫 IV 整族四函数（resolveNativeSpaceID=SA 直切空间号翻译 / staticDecodeSingleOrFirst=yabai 查询解析单一出口 / uuidFromDisplayID+fallbackUUIDFromHash=overlay 屏幕稳定身份）先行镜像转直测，新域 RunnerSpaceIdentityTests 9 断言（含宽容解码语义——缺字段成功解码且全 nil 的 yabai 版本漂移防御、UUID 大端位布局/哈希回退 abs(%256) 末字节/负值对称）；②B41 构建器族最后三缺口（itermInjectCommand 按 window id 的 current session 寻址+AppleScript 转义 / itermGetBounds 模板 / itermCreateWindow 空命令无 writeText 细节）入 RunnerTerminalDialectTests +3；生产加性改动：YabaiSpaceInfo 补合成 Equatable（四可选值字段行为等价——镜像当年自加 Equatable 的诉求由真身承接）；两镜像退役（Standalone 72→70）；Runner 与 B70 合并后 1020→1032 | ✅ 2026-09-08 |
| 角标编号同源（2026-09-09 用户反馈「minimap 索引跟内部对不上」） | 全应用编号源排查：设置域（minimap 屏N/S 标注/Space 胶囊/编排目标摘要）已全线 yabai 坐标系自洽，唯一孤儿=物理屏幕 overlay 角标——屏号用 NSScreen 枚举序（本机与 yabai 序相反）+ space 号用屏内位次（全局 3 在 2 胶囊屏上显示 2）。统一：①`OverlayWindow.calculateOverlayLabel` 屏号优先 yabai displayIndex（几何精确解析，nil 回退 NSScreen 序+1 旧行为）；②`resolveScreenSpaceIndex` 语义 位次→yabai 全局索引（唯一消费方=角标刷新，安全）；③`screenSpaceCache` 三元组纳入 yabaiDisplayIndex + `screenCacheChange` 比较扩展（插拔后 yabai 重排而 NSScreen 序未变也重绘）；④漂移镜像 Standalone ScreenSpaceIndexResolutionTests 退役，契约转 Runner 真身直测（overlayGate E +2 区分性断言 / E2 新增 5 断言 / pureSweep2 B4 更新），Runner 1032→1039 | ✅ 2026-09-09 |
| 角标/胶囊编号语言改「屏号-位次」（2026-09-09 用户第二轮反馈裁定：期望 1-1、3-1、3-2、2-1、2-2） | 上一轮把角标 space 位改全局索引属方向误判——用户要的统一语言是「屏号-屏内位次」。本轮：①`resolveScreenSpaceIndex` 回位次语义（保留 yabai 屏号修复=角标屏号与 minimap 屏N 同源，这正是用户第一轮抱怨的点）；②新增 `ScreenLayoutMapper.userVisibleSpaceLabel`/`positionInDisplay` 纯函数（RunnerLayoutGridTests +3 直测），minimap 胶囊文案/S 标注/悬停与无障碍文案/编排目标摘要/切换反馈 toast（GridSpaceSwitchFeedback 改 label 参数）全部改说「屏号-位次」；快照/yabai 缺失时各处回退全局号兜底；Runner 1039→1040 | ✅ 2026-09-09 |
| B72 | 漂移镜像再退役一条（B61/B67/B68 家法）：`GridTargetLogicTests`（532 行/全仓最大镜像）机械比对实锤**双料漂移**——①真身 GridTargetCode 已加 summaryText 四分支（RunnerSoundVoiceHookTests 已直测），镜像副本没有、穷举不到；②真身 MappedScreen 已加 yabaiDisplayIndex（屏号同源统一产物），镜像完全不认识该字段（测旧幻影）；③其 DisplayWorkArea/偏好语义段已被 B70/B68 前批直测覆盖为纯冗余。镜像独有缺口转真身直测：新域 `RunnerGridTargetLogicTests` 18 断言——GridTargetCode 编码端四形态+全 case parse↔code 往返+d1s 空数字+UInt32 上界/溢出+explicitDisplayID 四分支；TerminalGridPlanner.cells 无缝几何真身锁（2×2 共边/并集铺满/面积守恒、1×3 非整除共边+末格贴缘+整数格线+宽度差≤1、3×3 末行贴底、gap=8 恰缝+总宽守恒、gap=6 恰缝、负 gap 钳 0、非法行列/零面积→空）；ScreenLayoutMapper yabaiDisplayIndex 透传（漂移证明点）+4 胶囊等分 2px 缝+整条内嵌底缘+isVisible 逐格透传+visibleSpaceIndex 取首个可见+预览格线共边贴缘。镜像删除，Standalone 69→68；Runner 1075→1093 | ✅ 2026-09-09 |
| B73 | LAN 模块审计遗留项落地 + 新鲜四维扫描（TODO 0/镜像无漂移/最大文件 446 行 Doctor 系并行线增长在控）：`currentLANIP()` 去 en0 硬编码——旧行为 Wi-Fi 不在 en0 的机器退回 127.0.0.1（设置页 IP 显示与远程安装脚本 host 双失效）；提纯 `selectLANIP` 纯判定（en0 最优先 → 其它物理 enX → 排除 loopback/utun/awdl/bridge 虚拟口 → nil 调用方回退 127.0.0.1）+ Runner 直测 6 断言（优先序/排除集/空集）；合并并行 B72（GridTargetLogic 镜像退役 18 断言）后 Runner 1099/1099。**LAN 审计另一 nit 留档**：isRemote 日志分类依赖 X-Forwarded-For 代理头（直连 LAN 请求记 local），纯日志失真不改 | ✅ 2026-09-10 |
| B74 | YabaiModelTests（389 行）漂移镜像退役（原编号 B73 与并行 LAN IP 线撞号，让先合者改 B74）：机械比对实锤——镜像 YabaiWindowInfo 副本无 hasFocusRaw（真身 has-focus 守卫降级字段），全字段解码对真身新字段永久失明；YabaiSpaceInfo 副本仍是合成 Decodable（真身已换宽容 init(from:)，镜像根本测不到 int 形态不炸的语义）。既有直测不重复（minimized 双键/ax/SpaceInfo flexible 已在 RestoreOrchestration/PureSweepA，decodeSingleOrFirst 已在 B71）：真身剩余缺口转新域 `RunnerYabaiModelTests` 17 断言——WindowInfo 全字段黄金解码（11 键映射+四计算属性总锁）、has-focus bool/int/缺失三态（漂移证明点）、is-floating false/缺失/int 三态、minimized 双键并存 v7 优先+垃圾串不炸整体解码、Frame.cgRect、DisplayInfo 负 y+frame 缺失、formatErrorMessage 四路径、decodeArray 数组全量/单对象/垃圾三态（4 个查询路径共用出口）、ScriptWindowSnapshot Codable 回环+nil 字段。镜像删除，Standalone 68→67；与 B73 合并后 Runner 1099→1116 | ✅ 2026-09-10 |
| B75 | 成员级机械比对扫描全剩余镜像（61 文件类型抽取+逐成员集合差），三镜像退役（toggle/音效域，B68 多文件先例）：①`WindowStateTests`——镜像半死：isNearTarget 三断言测**真身已删的幻影 API**（现行判据=CoordinateKit 收敛族 B43 已直测）；hasToggleState 四断言转真身直测（RunnerToggleDecisionTests +2）。②`ProjectSoundResolverTests`——resolvedType 解析矩阵已 PureSweepB 全锁、CompletionSoundType 契约已 B61 直测，纯冗余；缺口转真身：`WindowManager.projectName(fromCwd:)` 提取族（末段/小写/尾斜杠/全斜杠 nil——resolver 双侧归一的唯一实现此前零直测）+ `ProjectSoundResolver.projectName` 双参优先级 + `ProjectSoundRule.effectiveSoundType` 兜底（生产注释自称「Runner 直测」实际零直测，注释失实一并修正）+ Codable 回环 → RunnerSoundVoiceHookTests +7；真身注释指针同步改向。③`SoundPlayGateTests`——决策表已 PureSweepB 全锁、SoundPreferences 兼容解码已 RunnerSoundVoiceHookTests 直测，且镜像的 SoundPreferences 是 soundType:String 旧形状（真身已 enum 化）=漂移冗余双实锤，纯删。Standalone 67→64；Runner 1116→1125 | ✅ 2026-09-10 |
| B76 | 帧写入域+屏号偏好域三镜像退役（B68 多文件先例；shortfalls/resendSegments 已在 ConvergencePipeline 真身全锁含 isFrameConverged 对拍、writeOrder 主干已在 RestoreOrchestration 直测）：①`FrameWriteOrderTests`（161 行）——writeOrder 分支边缘六缺口转真身直测（RunnerRestoreOrchestrationTests +5 断言：currentSize=nil 历史序防御、收窄单维触发×2、放大未传参/尺寸相等持平历史序、目标宽超源屏 clamp 规避、中间态贴合源屏可视区边界 contains 含等缘）；②`FrameResendPlanTests`（104 行）——纯删：镜像的 shortfalls 内联公式与真身 CoordinateKit 漂移和语义一致但测的是副本（两函数真身直测已齐，公式对拍已有）；③`ScreenIndexPreferencesTests`（304 行）——纯删：IndexPosition/CodableColor/loadLegacy/enforce 已在 PureSweepA 真身全锁（enforce 迁移分支带 save 副作用不直测已有既定结论）。Standalone 64→61；Runner 1125→1130 | ✅ 2026-09-10 |
| B77 | `SpaceContextLogicTests`（233 行）漂移镜像退役（restore 上下文域）：甄别三段——①镜像 `headerValue` **已死**（真身零定义零调用，hook 头解析改道）不移植；②`displayLocalSpaceIndex` 实例包装=日志+活查询 IO，纯判定早已抽缝 `resolveDisplayLocalSpaceIndex` static 而镜像仍测副本（副本 YabaiSpaceInfo 缺 isVisible 属旧形状）→ 语义经真身静态缝直测（RunnerSpaceContextTests 新域 12 断言：入参 nil 四组合/单 space 位次/多 space 屏内位次/多屏 display 过滤双态/索引不存在/乱序输入按 index 定位/nil index 沉底不占位/空数组 nil）；③`preferredSourceSpace` 经 SpaceController.shared 直调（B74 decodeArray 同法先例）锁四分支（漂移采 windowSpace/同值透传/单值透传/全缺 fallback 与 nil）。Standalone 61→60；Runner 1130→1142 | ✅ 2026-09-10 |
| B78 | CoordinateKit 值类型三镜像退役（CoordinateKitLogicTests 210+QuartzRectTests 120+QuartzConversionTests 68）：函数族（quartzY/cocoaY/clampFrame/收敛判据族）Runner 已有 60 处直测、镜像换算恒等式段纯冗余；真身缺口=三值类型本体契约零直测 → 新域 `RunnerCoordinateTypesTests` 10 断言——DisplayIdentifier 三体系 Equatable+yabaiIndex 取值（screen 序/cgDisplay 均 nil）+description 三形态；SpaceIdentifier 双体系 Equatable+yabaiIndex+description 两形态；QuartzRect 几何访问器/CGRect 回环/Equatable/日志描述族三属性（description/originDescription/sizeDescription=真身 2.16a 第十五刀唯一事实源，镜像副本缺 origin/sizeDescription 两成员=漂移实证；Int() 向零截断语义 -1.9→-1 锁定）。Standalone 60→57；Runner 1142→1152 | ✅ 2026-09-10 |
| B79 | 终端选择/自动恢复域双镜像退役：①`TerminalSelectionLogicTests`（194 行）——量表段（累加/排序/minCount/半衰衰减/回升）已被 B23 HookWalkTests 真身直测覆盖，纯冗余；缺口=自动评分四分支与常用警告函数（unsupportedFavoriteWarning 生产零直测）→ RunnerLayoutGridTests 选择器段 +8 断言（在运行加成压计数差/同态按计数/计数同按 lastUsedAt 新者/.none 不参与自动选择/全零数据 autoDefault 兜底 source 锁定；警告三分支=最常用 Warp 提示含名称/首条缺 candidates 记录跳过续扫次条/最常用可编排与低于阈值均无提示）。②`TerminalAutoRestoreLogicTests`（99 行）——纯删：命令/引号段与 RunnerLayoutGrid「脚本:」族同名同语义、规划器六分支与 RunnerLayoutGrid「规划器:」族同名同语义（真身直测先行存在）。Standalone 57→55；Runner 1152→1160 | ✅ 2026-09-10 |
| B80 | 五镜像退役轮（桌面网格/overlay 域，全部甄别后纯删为主）：①`RectangleFeatureLogicTests`（278 行）——摆位几何/网格规划（旧 cells 签名）/共存探测/session 定位四段全被 RunnerLayoutGrid+PureSweepA 真身直测覆盖（含非 jsonl 拒绝、空格点映射负例）；②`OverlayRefreshPolicyTests`（93 行）——门矩阵 8 组合中 6 组合已有直测，缺口转真身 +2 断言（门序 suspend 先于 disabled 的 suspend+disabled+非force 组合、distantPast 启动首次放行），C1~C5=平凡 Set 相等+filterStale（真身 screenCacheChange 5 直测+filterStale 已锁）；③`VoiceAnnouncementQueueTests`（92 行）——FIFO/满容丢最旧/容量防御/值语义已 PureSweepB 直测；④`GridSpaceDeliveryTests`（100 行）——决策矩阵已 RunnerSoundVoiceHookTests 直测 8 断言；⑤`CustomSoundStatusTests`（62 行）——五态已 RunnerSoundVoiceHookTests 直测。Standalone 55→50；Runner 1160→1162 | ✅ 2026-09-10 |
| B81 | 大文件循环再启：Doctor.swift 因 ax 自愈取证扩展回到 446 行（全仓最大），按域拆分——「安装副本盘点」域（InstallCopyInfo/RunningInstanceInfo/installInventoryLines 排版 + gatherInstallInventory 双安装位扫描 + detectSignatureKind 签名判定）整体迁至新文件 `Doctor+InstallInventory.swift`（121 行，extension Doctor 保持类型限定名不变、直测零改动），Doctor.swift 回 325 行单域（Journal/AX 取证 + 报告编排 + IO 辅助）；纯文件搬移零行为变更，Runner 1152/1152 全绿。**并行线动向**：B77（SpaceContext 镜像退役 12 断言）/B78（CoordinateKit 三镜像退役 10 断言）/B79（已占号未合）均为镜像退役线，本轮不撞；E2E 真机回归完成 SIZE/FLOATSETTLE 全绿 + GRID_SPACE/GRID_TARGET 环境受限注记（见 Tests/e2e/README.md 2026-09-10 节与 TCC 签名稳定化步骤） | ✅ 2026-09-10 |
| B82 | 浮动脱管/恢复链路双镜像退役（FloatToggleDecisionTests 113+RestoreRefocusCandidateTests 163；原编号 B81 与并行 Doctor 拆分线撞号，让先合者改 B82；B13 已把主干转直测，本批补边缘缺口后纯删）：镜像独有 4 断言转真身（RunnerRegistryStoreTests 决策块）——①floatToggle 守卫序：已 float 且无 AX 引用 → already_floating（floating 检查先于 unmanaged 的短路顺序此前只测过两分支各自独立触发）；②refocus minimized 字段缺失（旧版 yabai）按未最小化参与排序；③refocus 目标 space 无可管理窗口 → nil（不跨屏聚焦）；④preSwitch 单维缺上下文（sourceYabaiDisp=0）→ noContext（guard 双条件，此前只测过双零组合）。其余断言（决策序五分支/惰性求值/refocus 过滤/结局标签/重试处置/预切回主干）均为既有直测重复。Standalone 50→48；合并并行 B81（纯搬移零断言）后全量 Runner 1166/1166 | ✅ 2026-09-10 |
| B83 | LAN 提纯 + 三镜像退役（LANBindingTests 236/WindowSettleTimingTests 72/TTYNormalizationTests 81）：①生产提纯——LANHookPreferences getter 旧格式迁移分支抽为 `parseLegacyBindings(from:)` 静态测试缝（行为等价：Int/UInt32 双形态、垃圾值跳过），getter 写回语义不变，Runner 直测 +2 断言并修正原「归镜像覆盖」的失实注释指针；②`WindowSettleTimingTests` **重度漂移实锤**——镜像锁的旧固定时序模型（yabaiFrameWriteSettleMicros=400ms、yabai≥10×AX 关系）在真身已不存在，现行表为轮询制（frameVerifyPollIntervalMs 25ms+预算 400ms 等 8 常量）→ 现行基准值+两级关系不变量转真身直测 +3 断言；③TTY 精确匹配怪癖（"not a tty "尾随空格按路径补全）转真身 +1 断言，镜像退役。KeepaliveWrapperDecisionTests 甄别保留（生成物行为测试=测真实 install-keepalive.sh 产物，非漂移镜像）。Standalone 48→45；Runner 1166→1170 | ✅ 2026-09-10 |
| B84 | Mirror* 家族四镜像退役（FrameConvergenceLoopTests 266/FloatSettleSequenceTests 265/TerminalContextMatchingTests 240/ScreenIndexPreferencesMigrationTests 226）：①循环镜像的 convergeFrame 序列/短路/归一语义已 ConvergencePipeline C1~C6 真身直测，其调用点策略断言（attempts=2/settle=400ms 固定档）**已过期**——真身调用点全面轮询化（convergeFramePolling 25ms 节拍+预算），纯删；②FloatSettle 镜像的 A/B/C 段已有真身直测，缺口转真身 +3 断言（相邻两读漂移 8≤容差 1 拍稳定/漂移 21>容差不误判走满 12 拍/读全 nil 走满预算不崩溃且缓存仍清）；③终端上下文匹配镜像的 ps 解析段已 B66 直测，边界怪癖转真身 +4 断言（◂ 标记命中/普通连字符必须 em dash/标题侧大小写不敏感/空命令表与 nil 标题、多冒号按首切/空串恒真怪癖/四类注入拒绝、ttys 单数字与无编号宽松怪癖/换行注入）；④迁移镜像的 loadLegacy 主干已 PureSweepA 直测，解码失败边缘 +1 断言（非法 position 枚举/isEnabled 类型错误→nil），enforce 落盘分支维持真机验证结论。Standalone 45→41；Runner 1170→1179 | ✅ 2026-09-10 |
| B85 | 双镜像退役（YabaiEnvironmentProfileTests 233/CGWindowEntryTests 203）：①yabai 环境画像镜像——S/P/L 三段（float 布局保守策略/三层探测/候选路径）全部已被 PureSweepA probeProfile/probeParse/probeL1/probe编排 真身直测覆盖（生产 ShellRunner 注入式），纯删；②CGWindowEntry 镜像——该类型此前在 Runner 仅作夹具被间接消费，解码韧性契约零直测 → PureSweepA +3 断言（必填缺一不可+空 dict、windowID/ownerPID 字符串形态拒绝+layer/isOnScreen 缺省、kCGWindowName→name 双键回退+bounds 缺维补 0；顺带锁定 Int 字面量进 Any 后 as? pid_t 失败的强类型语义）。Standalone 41→39；Runner 1179→1182 | ✅ 2026-09-10 |
| B88 | 双镜像退役（原编号 B86 与并行远程安装线撞号，让先合者保留其号）（DataModelTests 231/ClaudeCodeWindowMatchTests 180）+ 一镜像甄别保留：①DataModelTests——TestTerminalContext 镜像的 hasUsefulContext（9 处直测已覆盖）/isRemote（RunnerSoundVoiceHookTests ctx 段已锁）/hasToggleState（B75 已直测）全部真身冗余，纯删；②ClaudeCodeWindowMatchTests——三级匹配主干已 RegistryStore claudeMatch 4 断言直测、projectName 主干已 B75 直测，边缘缺口转真身 +4 断言（matchClaudeCodeCandidate：策略1优先于策略2 的顺序锁/projectName 空串跳过策略1/'claude-coded' 子串不命中/多候选同中取首位/空候选 nil/谓词注入生效；projectName：根路径与双斜杠 nil/内部空段取末段/单段相对路径/带空格末段仅小写化）；③`HookSessionBindingTests` 甄别**保留**——resolveSessionBinding 的 healed/healLost 编排分支依赖 SessionWindowRegistry.shared 硬单例与真窗查询（verifyBinding→CGWindowList），单测域不可达，镜像+真机验证仍为该分支唯一覆盖，留待注入缝改造时再退役。Standalone 39→37；与并行 B86 合并后全量 Runner 1200/1200 | ✅ 2026-09-10 |
| B86 | **「远程一键安装」生成脚本实锤双 bug（原编号 B84 与并行镜像退役线撞号，让先合者改 B86） + 生成物行为测试补齐（B50/B54 模式）**：LAN 模块客户端侧（远程机器上执行的安装脚本）此前零存活测试。行为测试（沙盒 HOME + 受控偏好 + 真实 bash 执行生成的脚本）暴露两 bug：①**HOOKS_JSON 传整包裹 settings JSON**——jq `.hooks += $hooks` 合并后嵌套出 "hooks" 键，三个真正事件全部静默丢失（远程 hook 注册完全失效，正是用户 SSH 场景会踩的雷；此前服务器为手工注册故未暴露）；②无 jq 分支无法从 PATH 剥离 jq 验证（jq 在 /usr/bin）——测试改用沙盒 bin 符号链接。修复=新增 `generateHooksDictJSON()`（未包裹 hooks 字典）供安装脚本使用；Runner 新域文件 RunnerRemoteInstallTests 14 断言（label 派生/生成物字符串契约/jq 合并场景含种子 hook 保留与 forwarder 可执行/hook-config 四字段/无 jq 降级分支 settings 原样保留/全新 HOME 场景）；受控偏好先存后还原防 Runner 域污染。真机侧：Local Server 001 转发器为手工安装（等价正确），用户后续可改用一键脚本重装 | ✅ 2026-09-10 |
| B89 | 八镜像退役轮（小尺寸纯决策族）：①`FrameDescriptionTests`（QuartzRect 描述族 B78 已直测）②`FrameClampTests`（clampFrame 11 处直测）③`MoveToMainSkipDecisionTests`（Pipeline 19 处直测）④`GridSpaceSwitchFeedbackTests`（message 三态 PureSweepA 5 断言直测）⑤`FrameConvergenceTests`（originDrift/isFrameConverged B43 直测）⑥`ScreenYabaiDisplayMatchTests`（yabaiMatchDirect A/B/C 已锁）⑦`ErrorHandlingTests`——**死 API 实锤**：isExpectedQueryFailure/needsDetailedDiagnostics 真身零定义（现行判据=YabaiErrorClassifier，HookWalk 4 断言直测），纯删；⑧`MoveCooldownRegistryTests`——镜像 24 断言中边界/防御语义为真身缺口，转直测 +3（恰好 30s 严格 < 与 cooldown=0 关闭、未来时刻时钟回拨防御宽进严出、29.9s 向上取整与过期不返负）。Standalone 37→29；Runner 1200→1203 | ✅ 2026-09-10 |
| B90 | 七镜像退役轮（hook/restore 编排域，主干均系 B10~B50 批直测后的平行锁）：HookSettingsCompositionTests（识别/摘除/幂等生成 9 处直测）/HookWindowMoveRoutingTests（decideWindowMove 决策序+响应映射 14 处）/DisplayContextTests（displayContext 双坐标系+旧判据缺陷场景 8 处）/TerminalTreeWalkTests（walkToTerminalPID 父链/守卫/深度归一 13 处）/TerminalRegistryLogicTests（isTerminal 名单 4 处）/SAProbeAndPollingTests（saProbeVerdict 三层+ConditionPolling 节拍预算 26 处）/TitleEditorScriptDecisionTests（四分支模板+寻址铁律+哨兵 25 处）。`RestoreGuardTests` 甄别**保留**——非镜像，是「restore 路径禁加坐标校验」规则的守卫型元测试（无生产对应物，不构成漂移面）。Standalone 29→22；Runner 1203/1203 持平（零缺口批） | ✅ 2026-09-10 |
| B89 | hook 请求来源分类修真（LAN 审计 nit ①落地）：旧行为只认 X-Forwarded-For/X-Real-IP 代理头——直连 LAN 请求（本应用的主要远程场景）不带这些头，真实远程来源全被误记 local。新增 `classifyRequestSource` 纯判定（代理头优先 → TCP 对端 loopback 判定[127/8、::1、::ffff:127.*] → 缺失回退 local）+ `isLoopbackAddress` 纯函数；handler 经 GCDWebServer `remoteAddressString` 传入对端地址。直测 7 断言（C8a-g：代理头优先/代理 loopback/直连 LAN 主场景/本机 curl/IPv6 ::1 与 mapped/缺失兜底/判定矩阵）。合并并行 B89（撞号让先合者，八镜像退役轮）后 Runner 1210/1210。LAN 审计双 nit 全清 | ✅ 2026-09-10 |
| B91 | 三镜像退役转真身（TokenValidationLogicTests 121/SupportUtilityTests 146/PreferencesValidationTests 113）：①token 解析/校验（resolveProvidedToken/isTokenValid 真身静态测试缝）→ RunnerHookWalkTests +2 断言（query 优先回退 header/trim/大小写不敏感/双缺失空串；nil 与空串 expected 跳过验证/相等通过/不等与空 provided 拒绝）；②Support 日志三函数（sanitizeFieldValue 控制符转义与含空格加引号/serializeFields 键排序空键过滤/truncateForLog 260+省略号）→ PureSweepA +3；③normalizePort 边界族（0/负/1023→1024，65536+/1000000→65535，区间内原样）与 String.sanitizedForShell POSIX 转义 → PureSweepA +2。Standalone 22→19；Runner 1210→1217 | ✅ 2026-09-10 |
| B92 | 三镜像退役（YabaiErrorClassifierTests 118/ExitJournalTests 151/SupportMiscTests 137）：①分类器镜像——四类主特征已 HookWalk errClass 直测，补锁转真身 +2 断言（windowNotFound 与 unrecognized 兜底、大小写不敏感、多特征命中取表序最前的优先级语义）；②ExitJournal 镜像——jsonEscape 段与 RegistryStore 重复，诊断路径角色隔离真身缺口转直测 +1 断言（主应用规范路径/其它进程 -后缀/空进程名）；③SupportMisc 镜像——**生产提纯**：verboseLoggingEnabled 加载闭包内联 env 解析抽为 `parseVerboseLoggingFlag(_:)` 测试缝（行为等价，"1"/"true"/"yes" 大小写不敏感），连同 elapsedMilliseconds 取整语义转直测 +2 断言（env 端到端仍归进程启动快照，不可进程内测已如实注记）。Standalone 19→16；Runner 1217→1222 | ✅ 2026-09-10 |
| B94 | 六镜像退役（原编号 B93 与并行直测补齐线撞号，让先合者保留其号）（toggle 兜底/播报/解析域）：①`ToggleRouteTests`（route(for:) 18 组合语义表已 ConvergencePipeline 13 处直测）②`VoiceAnnouncementTemplateTests`（interpolate 9 处直测）③`RestoreAnnouncementPlanTests`（outcome→plan 总映射 18 处直测）④`SpaceSnapshotParsingStandaloneTests`（Bool/Int 双形态+缺字段跳过已 RegistryStore overlayGate D 直测）⑤`IPSParserTests`（parseIPS 5 处直测）⑥`ToggleFallbackWindowTests`——pickFallbackFrontWindow 全注入纯函数此前 Runner 零直测 → RunnerToggleDecisionTests +3 断言（z-order 首个合格者/自身 overlay·非零 layer·离屏·1x1 占位·非 regular 激活策略五重排除/无合格候选 nil）。Standalone 16→10；与并行 B93 合并后全量 Runner 1230/1230 | ✅ 2026-09-10 |
| B93 | 新鲜零命中扫描增量闭环（原编号 B92 与并行三镜像退役线撞号，让先合者改 B93）（47 候选 → 决策形 3 个）：并行重构产物直测补齐——`SoundPlayGate.isInQuietHours`（private→internal 直测：同日窗起闭右开/跨午夜 OR 语义/退化 start==end 常假）+ `ProjectSoundResolver.ruleMatches`（双侧 projectName 归一比较/不同项目 false/全斜杠归一 nil → false）；`findTerminalPID` 豁免（纯行走 walkToTerminalPID 已直测的 IO 薄壳）；generateHooksDictJSON 由 B84 生成物行为测试间接覆盖。合并并行 B91 三镜像退役轮后基线 Runner 1222/1222 | ✅ 2026-09-10 |
| B95 | 最后两枚大镜像退役（CoordinateValidationTests 305/HookConfigTests 258）：①isValid 域——镜像为 ToggleRecord.isValid 的 Quartz↔Cocoa 换算大夹具副本，B68 RunnerToggleDecisionTests 已七断言直测（含 max 缘边界），纯删；②HookConfig 镜像——normalizePort 已 B91 直测、endpointURLString/cleanVibeFocusHooks/generateHooksDict 已 B73-lanip/RemoteInstall 线直测，generateGeneratedToken 真身零定义（死 API 幻影），纯删。**镜像账收官**：Standalone 69→8，全部余量均为甄别保留（ArchitectureConformance/RestoreGuard=架构与规则元测试；KeepaliveWrapper/InstallRestartHardening/BundleIdentityContract=生成物行为测试；DoctorReport/HookSessionBinding=单测域不可达编排锁）。Standalone 10→8；Runner 1230/1230 持平 | ✅ 2026-09-10 |
覆盖率复测（B95 后，2026-09-10 清晨）：行覆盖 **20.59%**（28090 行基数；函数 32.42%、区域 29.78%），
较 B50 后 **+2.50pp**——B72~B95 漂移镜像退役 61 文件（测幻影的负资产清偿）+ 真身直测 +~123 断言
为本轮增量主因（Runner 1075→1230）。热点结构结论不变：文件级行覆盖不再作为批次目标（B40/B50
清账结论沿用），增量继续走「决策提纯→Runner 直测」模式。测量命令 `bash scripts/coverage_test_runner.sh`
（llvm-cov 真实数字，非代理指标）。
| B96 | **hook-forwarder.sh 转发器行为测试补齐（原编号 B94 与并行镜像退役线撞号，让先合者改 B96）（LAN 链路客户端核心，此前零测试）**：假 curl PATH 注入（沙盒 bin：python3/cat 符号链接 + 假 curl 逐参落日志），三场景真执行——①config 四字段 → POST 到 `http://$host:$port/claude/hook` + `X-VibeFocus-Token` 头 + enriched 载荷（terminal_ctx.machine_label/term_session_id/claude_project_dir 注入实证）；②token 空 → 无鉴权头（本机默认形态）；③非 JSON 载荷 → enrich 失败原样转发不吞事件。生成器字符串契约断言（配置读取/端点构造）。沙盒 bin 需含 cat（`VF_PAYLOAD=$(cat)` 读 stdin，set -e 下缺失即 127 中止——诊断输出实锤）| ✅ 2026-09-10 |
| LAN 全链路真机冒烟（2026-09-10，B89 修复后运行态验证） | ①本机部署产物：`~/.vibefocus/hook-forwarder.sh` 发射 UserPromptSubmit → 服务端 200 `no_binding_skip`（无绑定会话诚实跳过）；②远程部署产物：Local Server 001（192.168.1.83）经装好的转发器发 SessionStart → 本机收到 `source=remote(192.168.1.83)` `isRemote=true` `machineLabel=remote-server-001`（B89 分类生产生效）+ 诚实 409 `remote_binding_failed`（label 未映射窗口的预期响应）；③绑定自愈观察：指向已关闭测试窗（7280）的 remote-server-001 绑定被应用自动清理（totalRemoteBindings 归零）——死窗口绑定自清理为正确行为；**用户待办一步**：SSH 终端窗就位后，局域网 Hook 卡添加 `remote-server-001` → 「映射当前窗口」即完成联动闭环 | ✅ 2026-09-10 |
| B97 | performRestore 分阶段函数提取（行为等价重构，阶段 1/3/3.5/3.8/4a/4b/5 的内联体收敛为三个私有静态阶段函数；调用序列由 Tests/Runner performRestore 序列锁穷尽锁定，合前 1236/1236 全绿复核）：①`performSourcePreSwitch`——视角基准采集+源屏 space 预切回（sourceSpacePreSwitch 裁决+SA 直切/聚焦带动双层+等到位轮询）+守卫候选预取，返回 RestorePreMoveContext；②`performFloatDetachAndFrameMove`——FloatSettle 脱管等待+yabai abs 直写+moveMs 计时；③`performMoveFailureStage`——失败路径视角守卫+可重试性判定+屏外夹进源屏幂等重试+永久失败清 record（返回 RestoreOutcome）。performRestore 编排体 316→约 140 行，只保留阶段编排与成功尾段（lookup/query 计时与 completed 汇总留原地）；附带修复并行遗留 RunnerRemoteInstallTests 未使用变量警告（门禁零警告恢复）。行为零变更：三分支结局/审计事件/record 处置/日志字段逐项不变。Standalone 8/8；Runner 1236→1236 持平（结构批次） | ✅ 2026-09-10 |
| B98 | SessionWindowRegistry.bind 身份合并纯决策提纯（原内联 if-branch：别名冲突/同 session 刷新/新建三态混在持久化编排里）——抽为 `makeBoundState(existing:...) -> BindOutcome` 静态纯函数（.alias(existingSessionID)/.merged/.created），bind 只留 IO 编排（TerminalRegistry 门/AX windowNumber 解析/别名落表/lastEventDescription/prune/persist，行为逐行等价）；RunnerHookWalkTests +4 断言（created 全字段就位含 axWindowNumber/tty/cwd/model、他 session 活跃绑定→alias 不覆盖、同 session 重绑 merged 刷新并复活、已完成绑定可被新 session 复用）。SessionWindowRegistry 是覆盖率报告最薄核心文件（函数 43%），本次合并入段为后续深测铺缝。Standalone 8/8；Runner 1236→1240 | ✅ 2026-09-10 |
| B99 | pruneExpiredWindowStates 保留期决策直测补齐（临时库 B32 模式）：SessionWindowRegistry 的过期清理落库判定此前仅由覆盖率报告标注未测——活跃 24h/完成 4h 双保留期参数化注入，+2 断言（四态夹具[活跃 1h/活跃 27h/完成 2h/完成 5.5h]恰清 2 条过期且存活者可查、二次执行幂等 0）。Standalone 8/8；Runner 1240→1242（笔误更正：前行误写 1230 基线） | ✅ 2026-09-10 |
| B100 | SessionWindowRegistry 状态族注入式直测补强（B98 makeBoundState 同域续作）：临时库实例（init(store:) 注入缝，B32 模式）锁定 touch（updatedAt 前移+message 上 lastEventDescription）/markCompleted（完成置位+completedAt+别名清理+描述）/reactivate（复活复位）/setLastEventDescription（纯空白拒写）/remapWindowID（内存重映射+DB 同步双写）五成员语义 +5 断言——此前这些成员仅经 shared 单例字典播种被间接消费。Standalone 8/8；Runner 1242→1247 | ✅ 2026-09-10 |
| B101 | performRestore 成功尾段提取（B97 分阶段收尾）：6 视角守卫 + 7 清 record + completed 汇总日志（lookup/query/move/focusSpace 四段计时）+ restore_success 审计收敛为 `performSuccessTail` 私有静态函数——performRestore 编排体至此仅剩「加载→AX 探测→最小化快检→五阶段委派→结局透传」的纯编排（约 95 行），行为零变更由序列锁 1247 全绿复核。同批移除提取后失效的两个局部别名（零警告门禁）。Standalone 8/8；Runner 1247/1247 持平（结构批次） | ✅ 2026-09-10 |
| B102 | SessionWindowRegistry+Lookup（覆盖率最薄 29.63%）直测补强 +3 断言（注入式临时库实例）：activeBindingsForUI 只含活跃且 createdAt 降序 / recentCompletedBindings 仅 30 分钟内完成且 updatedAt 降序 / findState 内存 miss 时 DB 装载并回填缓存（附 init isTerminalPID 清扫语义实证——非终端 pid 行会被 init 删除，故夹具须先建实例后入库）。至此 SessionWindowRegistry 三 extension（State/Lookup/主体 bind）全部有注入式直测。Standalone 8/8；Runner 1247→1250 | ✅ 2026-09-10 |
| B103 | 双薄面直测补强（TerminalUsageTracker 40%/HookWindowModels 50%）：①用量表持久化/解码成员转直测——encoded→decode 回环保真、垃圾字节 nil、`TerminalUsageTracker(table:)` 注入式 init + seedUsage 直写计数与 lastAt、saveTable/loadTable UserDefaults.standard 独立域往返（Runner 进程域与真机隔离，B22 先例，用后清键防污染）→ HookWalkTests +4 断言；②WindowState originalFrame/targetFrame 取值器——四元组成帧、缺 target 侧 targetFrame 为 nil → RunnerToggleDecisionTests +1 断言。Standalone 8/8；Runner 1250→1255 | ✅ 2026-09-10 |
| B104 | 薄面补测收尾（SessionWindowRegistry+Lookup / ClaudeHookPreferences）：binding(for:) 别名通道（sessionAliasWindowID 解析到主绑定）与 persistToDB 落库往返 → HookWalkTests +2 断言；endpointURLString 双态（无 token 纯端点/配置 token 追加 ?token= 查询串，用后清键防域污染）→ +2 断言。Standalone 8/8；Runner 1250→1259 | ✅ 2026-09-10 |
| B105 | captureLayout 窗口过滤提纯（TerminalGridController+Capture 16.28% 最薄面）：内联 filter 闭包抽为 `isCapturableTerminalEntry(_:targetDisplayID:bundleIDOf:displayIDOf:)` 静态纯决策（layer 0+onscreen+≥100pt 合理尺寸+属目标 display+owner 编排支持终端五重判据；bundleIDOf/displayIDOf 双闭包注入，生产=实例 IO 方法），captureLayout 只留 IO 编排；RunnerTerminalGridUnitTests +4 断言（合格通过/非零 layer·离屏·小窗 <100pt 拒绝/owner 非终端拒绝/目标 display 不符拒绝）。Standalone 8/8；Runner 1259→1263 | ✅ 2026-09-10 |
| B106 | selectionPreview 候选装配提纯（TerminalGridController+TargetResolve 0% 覆盖面中的纯决策段）：supportTable 全量名单→候选装配（usage/lastUsed/running 观测接线）+ appPreference→manualBundleID 映射抽为 `selectionPreview(runningBundleIDs:usageRank:appPreference:)` 静态纯函数，实例方法只剩 NSWorkspace/UserDefaults 采集（IO 归 E2E）；RunnerHookWalkTests +4 断言（.terminal/.iterm2 manual 映射、运行优先于高用量的观测接线、全零观测 autoDefault 兜底）。Standalone 8/8；Runner 1263→1267 | ✅ 2026-09-10 |
| B107 | resolveRemoteBinding 真实查找路径直测（HookEventHandler+Remote，远程自愈入口）：label 未映射 → nil 且 shared 描述更新（「未映射到窗口」）；label 已映射但窗口已消失（findWindowByCGWindowID miss）→ nil——两分支此前仅 HookWalk decideSessionBind 纯判定覆盖、真实查找路径未测；remoteBindings 种子化进 Runner 独立 UserDefaults 域、saved/清键防污染（B22 模式），handler 经 .shared 单例调用（init private）。hookConfigJSON 已有 2 处直测确认不重复。Standalone 8/8；Runner 1267→1269 | ✅ 2026-09-10 |
| B108 | binding(for:) DB-fallback 脏数据清理分支直测（SessionWindowRegistry+Lookup 收尾）：先建实例后入库（绕过 init isTerminalPID 清扫，B102 实证）+ 非终端 pid 脏绑定 → 返回 nil 并从库删除（corrupt cleanup）+ 二次查询幂等 nil，+2 断言。Standalone 8/8；Runner 1269→1271 | ✅ 2026-09-10 |
| B109 | BindingType 持久化契约直测（bindingType 落 SQLite/审计行的 rawValue 稳定性）：rawValue local/remote 契约 + 非法值拒绝 + JSON Codable 回环保持，RunnerHookModelsTests +2 断言。Standalone 8/8；Runner 1271→1273 | ✅ 2026-09-10 |
覆盖率终测（B109 后，2026-09-10 09:00 冲刺收官时点）：行覆盖 **21.73%**（28146 行基数；函数 33.56%、区域 31.28%），较 B95 时点 20.59% 再 **+1.14pp**、较 B50 基线 **+3.64pp**——增量来自 B97~B109（performRestore 分阶段/注入式状态族与 Lookup 直测/selectionPreview 与 capture 过滤提纯/usage 持久化与 BindingType 契约/resolveRemoteBinding 真实路径）。SessionWindowRegistry 主体已从 43% 函数覆盖升至 51%；热点结论沿用（余下低覆盖均为 IO 编排，纯内核已全部直测）。
| B111 | pruneExpiredBindings 内存+DB 双层清理直测（SessionWindowRegistry 保留期 24h 活跃/4h 完成）：四态夹具（活跃 25h/1h、完成 5h/2h）经 store 行+内存字典双播种（先建实例后入库绕 init 清扫），锁「DB prune removed>0 才触发内存同规则过滤」的耦合语义与幂等性，+2 断言。Standalone 8/8；Runner 1273→1275 | ✅ 2026-09-10 |
| B112 | mergedHooks 合并+开关裁剪直测（CodexHookPreferences/CodexHookInstaller 52.63% 薄面）：全新安装三事件就位且外部键保留 / 开关关闭移除 SessionEnd·UserPromptSubmit 而 Stop 留存 / 幂等重装不叠加（键集一致），HookWalkTests +3 断言。Standalone 8/8；Runner 1275→1278 | ✅ 2026-09-10 |
| B113 | 薄面收尾微批（Codex 路径/安装检测 + lanMode 往返 + supportTable 全表）：codexConfigDir/Path home 注入派生（.codex/hooks.json）、isHookInstalled 语义直测（command 含 helperScriptPath → true、缺失/无匹配 → false，注意 createFile 不建中间目录）、lanMode set/get 往返（独立域用后复位）、supportTable 九终端全表逐项锁定（新增/删除/改级都红）→ HookWalkTests +5 断言。Standalone 8/8；Runner 1278→1283 | ✅ 2026-09-10 |
| B114 | uninstallHookFromCodexSettings 卸载路径直测（CodexHookInstaller 卸载按钮路径）：混合设置（我方 url 条目+外部 command 条目+他方顶层键）卸载后我方条目清除、外部条目与他方键保留、返回成功；缺文件与坏 JSON 均按「视为已卸载」返回 true（实现语义如实锁定）→ HookWalkTests +3 断言。Standalone 8/8；Runner 1283→1286 | ✅ 2026-09-10 |
| B115 | 收尾微批：clearAllBindings 四清直测（内存/别名/描述/DB）+ TerminalUsageTracker.start 二次调用幂等 → HookWalkTests +2 断言。Standalone 8/8；Runner 1286→1288 | ✅ 2026-09-10 |
| B116 | binding(for:) 直命中/alias 优先级与无命中 nil 直测（单条目确定性夹具），HookWalkTests +2 断言。Standalone 8/8；Runner 1288→1290 | ✅ 2026-09-10 |
| B117 | generateHooksDict 三开关组合直测（HookInstaller/ClaudeHookPreferences 交界默认字典）：三开关全开四事件键齐且条目嵌 hooks 含 helper 命令 / SessionEnd·UserPromptSubmit 开关关不注册而 Stop 恒注册（handleStop 内部按 remoteOnly 分流的产品语义）→ HookWalkTests +2 断言（偏好独立域种子化用后还原）。Standalone 8/8；Runner 1290→1292 | ✅ 2026-09-10 |
| B118 | 生产注释指针一致性收尾（B72~B116 镜像退役的收尾清扫）：全仓扫描发现 13 文件 17 处生产注释仍指向已退役镜像文件（开发者按注释找测试会扑空）——逐处改指真身直测的 Runner 域文件（TitleEditor+ScriptDecision/SoundPlayGate/ToggleEngine+Restore+Decision×4/TerminalRegistry/FloatSettle/FrameConvergence×2/WindowManager+TerminalContext+Helpers/WindowManager+Restore/SpaceController+Move/CoordinateKit/OverlayRefreshPolicy/TerminalGridController+SpaceDelivery/HookSettingsComposition）；Tests/Runner 域文件头部的历史溯源注释（「XxxTests 镜像退役」叙事）与 Tests/XCTest 休眠套件自引用属档案性质不改；docs 历史文档按 B62 惯例保留原貌（台账为唯一现行事实源）。纯注释变更零行为；终扫 Sources 零残留。Standalone 8/8；Runner 1292/1292 持平 | ✅ 2026-09-10 |
| B119 | architecture-baseline.md 现行状态文档指针更新（B118 同域收尾）：模块-测试锁对照表中 6 处指向已退役镜像（QuartzConversionTests/WindowSettleTimingTests/FrameWriteOrderTests/FrameConvergenceLoopTests/FrameResendPlanTests）与 3 处叙事引用改为真身直测位置（RunnerRegistryStoreTests coord/settle/overlayGate 块、RunnerRestoreOrchestrationTests writeOrder 块、RunnerConvergencePipelineTests convergeFrame/shortfalls/resendSegments/floatsettle 块）；退役溯源注记按惯例保留。code-quality-playbook.md 按 B62 惯例属历史叙事不改。纯文档变更；Runner 1292/1292 持平 | ✅ 2026-09-10 |
| B120 | ensureTokenGenerated 生成/缓存契约直测（ClaudeHookPreferences 认证 token——token 稳定性有安全意义：轮换会使远程机器端配置的 token 失效）→ HookWalkTests +3 断言（缺失时生成 32 位小写十六进制并持久化/已有原样返回不轮换/清空后重新生成为新随机值；独立域种子化+saved 还原防污染）。来源=B120 全 Sources 零引用函数扫描（36 候选甄别，余者全为 NSWorkspace/getifaddrs/GCDWebServer/AppleScript IO 归 E2E）。Standalone 8/8；Runner 1292→1295 | ✅ 2026-09-10 |
